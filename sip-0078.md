```
SIP: 78
Title: An origin that does not answer
Author: Colin Lyons <wave-cl@pm.me>
Status: Draft
Type: Standards Track
Layer: Exchange
Created: 2026-09-19
Requires: 35, 40, 43, 53, 56, 59
```

## Abstract

SIP-35 §What a stranger can cost has an exchange answer a peer that is
over its peering-write
limit with `rate_limited` and the seconds to wait, and says nothing
about what the peer does with that. The reference peer did nothing: it
logged the code where nobody looks, went on writing into a closed
window, and when a member's own act was refused behind it, told the
member the origin was away. This document says what a peer MUST do
with a limit -- hold its writes to that origin for the seconds given,
pass a limit on a member's act to the member as a limit, say so where
an operator looks, and answer `/status` with what each origin last
refused and why -- and with an origin that gives no answer at all: back
off from it, doubling from one cycle to an hour and resetting on the
first answer, never holding a member's own act, and list every origin
the home has a reason to contact, an unfound one with how long, how
often and when next.

## Motivation

The live run of SIP-60 §A device hints its home (2026-09-19). A home
carried a Move it had carried a hundred times
before; the origin, over the home's hour of writes, answered
`rate_limited`; the home's log said, at a level the journal did not
keep, *the origin did not take the move*; the home carried the create
behind it; the origin refused the create for want of the Move, with the
uniform refusal, which the home relayed to the member as `origin_away`.
The member's client said the origin could not be reached. The origin
was reached four times in that second.

Every party did what its document said. SIP-35 gave the limit a
distinguishable answer *so that* a peer could act on it, and no
document said what acting on it is. SIP-56 gave a client the seconds to
wait for its own acts, and no document carried that number across the
hop a copy adds. Three live runs read the symptom as a client timeout
at a different layer.

*Added 2026-09-19, was SIP-80.* The finding, from the day after §Holding off shipped (0.96.0). An account homed at
trunk.exchange had, two days earlier, moved there from a laptop
exchange and named the laptop as an origin, under a domain that was
never published. The laptop was stopped the same evening. From then
until somebody read the journal for another reason, trunk.exchange
resolved that domain every thirty seconds -- 10,945 lines in two days,
the DNSSEC resolver's warning among them -- and, once SIP-68 collected mail
and backups, tried those pulls behind it each time.
Nothing was broken; nothing was bounded either. `sqex status` at
trunk.exchange listed one origin, the one it had reached. The origin
that had cost it twenty thousand lookups was not on the list, because
the rows as first written were made of what an origin *said*, and this
one had never said anything.

The limit above and this are the same shape: an origin's non-answer,
retried on the clock. The limit bounded the case where the origin
speaks. This bounds the case where it does not, and
puts it where the other one is now visible.

## Specification

All integers are unsigned big-endian. **Peering writes** and the
`peering` limit are SIP-35 §What a stranger can cost's; `rate_limited`
and its detail are
SIP-56's; a **forwarded act** is SIP-43's (a post, an action, a create
carried to the origin for a member at a copy or a home); the **home
task** is SIP-59's. The **home task**, its **cycle**
(`home_secs`) and an **origin hint** are SIP-59's; **reach** is
SIP-60's; `origin_away` and **away** are SIP-53's.

### Holding off

A peer answered `rate_limited` on a peering write to an origin MUST
make no further peering write to that origin until the seconds in the
detail have passed, measured from the answer; its reads -- pulls,
waits, `/peer/mine`, standing -- are not limited (SIP-56 §Rate limits)
and continue. Writes it would have made in the window are made after
it, where they still apply: a Move is carried when its turn next comes,
a telling that was due is told. A peer that cannot parse the detail
holds off for `HOLD_DEFAULT_SECS`.

### Passing a limit through

An exchange forwarding a member's act (SIP-43, SIP-60) that is answered
`rate_limited` by the origin MUST answer the member `rate_limited` with
the same detail, as SIP-56 has the member's own exchange answer, and
MUST NOT answer `origin_away`, which is for an origin that did not
answer. The member's client shows the wait. The peer holds off its own
peering writes as above only where the limit was the *peering* one;
the origin's limits on the member's acts (`posts`, `creates`, ...)
are the member's and hold nothing at the peer.

An exchange forwarding a member's act refused for want of something the
peer failed to carry -- the uniform refusal after a Move the origin
would not take -- answers `origin_refused` (SIP-21 code 70), a refusal
distinct from `origin_away`: the origin answered, and said no.

### Unfound

*Added 2026-09-19, was SIP-80.* An origin is **unfound** at a home from the moment a home-task cycle
fails to obtain a connection to it -- because no address can be had for
its key or hinted domain (**no address**), or because a connection to
the address it has was not established (**unreachable**) -- until the
home next obtains one, by any path: a later cycle, a forwarded act, a
pull. The home records, per origin, when it became unfound, how many
consecutive cycles have failed, why the last one failed, and when it
will try next.

### Backing off

After the `n`th consecutive failure the home makes no attempt to find
or connect to that origin for

```
hold = min(home_secs × 2^(n−1), MAX_UNFOUND_SECS)      MAX_UNFOUND_SECS = 3600
```

measured from the failure. The first failure therefore holds for one
cycle, which is what happened before this section; the reference
default's eighth holds for an hour, and every one after it does. The
hold covers everything the home task would do with that origin in a
cycle: carrying Moves and folds, pulling channels, mail and backups.
Nothing is dropped: what was due is done on the first cycle after the
hold, as §Holding off has writes done after a limit's window.

**A member's act is never held.** A post, create or action a member
forwards through the home is carried as SIP-43 carries it, whatever the
origin's state: it is the one attempt a person is waiting on, and its
success ends the hold. Its failure is answered as SIP-53 and §Passing a
limit through already say (`origin_away` with the seconds since the origin last
answered) and does not count as a cycle.

An origin that answers -- a connection established and its hello read
-- is no longer unfound, the count is zero, and the next failure starts
the doubling again from one cycle.

### A configured origin

The same hold applies to an origin the operator configured for
replication (SIP-35), whose loop otherwise retried a dead address every
`interval_secs` and said so each time: after the `n`th consecutive
connection failure it waits `min(interval_secs × 2^(n−1),
MAX_UNFOUND_SECS)`, says so once per hold, and is listed in `/status`
as unreachable. A poke -- a member's act carried through the forwarder
-- still ends the wait early, for the reason a member's act is never
held. *(Added 2026-09-20, sqex 0.100.0; the first release held off the
home task's origins only.)*

### Saying so

A peer that is answered `rate_limited` by an origin, or whose write to
an origin is refused, MUST record for that origin the last refusal --
the path, the code, the wait where given, and when -- and MUST log it
at a level an operator's journal keeps, once per origin per window,
not once per request.

`GET /status` (SIP-9) gains:

```
"origins": [ { "key": ..., "domain": ..., "reached": <secs ago | null>,
               "held_until": <unix | null>,
               "last_refused": { "path": ..., "code": ..., "wait": ..., "at": ... } | null } ]
```

one row per origin the exchange forwards to or pulls from, in no
particular order. `reached` is when the origin last answered anything
(SIP-53 keeps it); `held_until` is the end of a hold in force;
`last_refused` is the record above, and stays until replaced -- a
refusal is a thing to have seen, not a thing to have been cleared.

*Added 2026-09-19, was SIP-80.* Each time a hold is set the home says so once, where an operator looks
(warn), with the origin, the hinted domain, why it was not found, how
many consecutive cycles have failed, and the seconds until the next
try. Between holds it says nothing.

The origins rows list **every origin the home has a reason to
contact**: those it forwards to, those it has reached, those
that refused it, and those hinted by any account homed here. A row for
an unfound origin carries

```
"unfound": { "since": u64, "tries": u32, "next_in": u64, "why": "no_address" | "unreachable" }
```

-- when it was first missed, the consecutive count, the seconds until
the next attempt, and the last reason -- and is absent from a row whose
origin is not unfound. `reached` stays what it is above: seconds
since the origin last answered, absent where it never has.

### Hints

A hint is the account's word about where its conversations live
(SIP-59) and this section does not expire one: an origin can be down
for a week and back, and SIP-60 §What a fold record does already gives a folded
conversation seven days. What a stale hint costs is now one attempt an hour, and
what it looks like is a row in `/status` with `unfound.tries` in the
hundreds, which is the operator's cue to ask the account to Move again
without it -- SIP-59's `record_move` clears every hint the account gave
before.

### Limits

| Limit | Value |
|---|---|
| `HOLD_DEFAULT_SECS` | 60 |
| Origins in `/status` | 256 |
| `MAX_UNFOUND_SECS` | 3600, `MAX_HOLD_SECS` |
| Unfound records | one per origin the home has reason to contact |

## Rationale

**Why hold off rather than retry.** The origin said when. A peer that
retries before then spends the answer it was given and, under SIP-35's
bucket, pushes the window further out; a peer that waits exactly as
long as it was told wastes nothing.

**Why the limit is passed through unchanged.** The number is the
origin's, about the origin's bucket, and the member is the only party
that can act on it -- by waiting, or by not sending. Turning it into
`origin_away` told the member to look for a network fault that was not
there, which is the failure the motivation records three times.

**Why `origin_refused` is a new word.** SIP-21's `origin_away` was
written for an origin that does not answer, and a client is told to
treat it as a copy that cannot reach its origin -- to wait, to try
elsewhere. An origin that answers *no* is a different situation with a
different remedy, and a client that cannot tell them apart cannot
choose it.

**Why the record stays.** A refusal that clears itself on the next
success is a refusal an operator sees only if they were watching at
the time. The status line is read afterwards.

**What was rejected.** Having the origin lift its limit for listed
peers: SIP-35 already lets an operator set it to unlimited, and the
peer's behaviour must be right under any limit. Retrying the write
behind the refused one with the Move re-carried first: the peer does
not know which write the origin wanted, and a Move it carried once is
recorded (SIP-60's reference implementation, 0.94.1, made the carry
once per Move).

*Added 2026-09-19, was SIP-80:*

**Why doubling from one cycle, and not a fixed hold.** An origin that
is restarting is back in a cycle or two, and a fixed hold of any useful
length would make every restart cost that long. Doubling costs nothing
for the first miss, little for a short outage, and bounds the stale
case at an hour, which is where §Holding off bounds a stated wait. One
constant, shared with the hold, and one rule.

**Why a member's act is not held.** §Holding off held a *home's* writes
and let a *member's* act through with the origin's own answer; the same
line holds here. A person who posts is waiting; an exchange's cycle is
not. And the member's attempt is the cheapest probe there is: it
happens only when somebody wants something, and its success clears the
hold without the home having to guess when to look.

**Why the rows list hinted origins.** The rows as first written were
built from what an origin said, so an origin that never spoke was not in them --
the exact origin an operator needed to see. The hints are the reason
the home is trying at all; listing them makes the rows the answer to
"what is this exchange trying to reach, and how is that going", which
is the question §Saying so meant to answer.

**Why not expire the hint.** Stated above; and because the cost of
keeping a stale one is now bounded and visible, while the cost of
dropping a live one -- an account whose origin was down for eight days
finding its conversations gone from its home -- is not.

## Compatibility

Additive: two status fields, one refusal code, and rules for the peer.
An origin from before SIP-35 §Open peering (0.80.0) answers the
uniform refusal for
everything, and a peer holds off for nothing it cannot recognise. A
client from before this document treats `origin_refused` as a refusal
it does not know the word for, which SIP-21 tells it to show as such.

*Added 2026-09-19, was SIP-80.* Additive. No wire change: the hold is a home's own scheduling, and the
`/status` field is new. A home from before §Backing off (0.98.0) tries every
cycle. An origin sees fewer attempts and nothing else.

## Security considerations

**A lying origin can hold a peer off.** An origin that answers
`rate_limited` with a large wait stops a peer writing to it for that
long. It could refuse every write outright already; the wait is a
courtesy about its own state, capped at the peer's discretion.

**`/status` discloses which origins refused this exchange.** As it
discloses its peers already (SIP-9); the paths and codes are about
this exchange's own requests.

*Added 2026-09-19, was SIP-80.* **What is disclosed.** `/status` is public (the origins rows are, on the
ground that an origin's key and domain are what SIP-59
discloses to anyone who asks where an account lives). A row for an
unfound origin adds that this home cannot reach it and since when --
which anyone who tried the domain could also learn.

**Denial.** An account can hint any key and domain it likes and make
its home try them; that was so before, at one attempt per cycle
forever, and is now one an hour after a few minutes. A home's
attempts to reach an origin are bounded per origin, and the origins are
bounded by SIP-60's `MAX_HINTS` per account.

**A hold is not a verdict.** An origin under a hold is *away* in
SIP-53's sense, not refused; a member reading a copy at the home reads
what the home holds, as SIP-53 says of an origin that is away, and a
member's act still goes out.

## Reference implementation

`sqex` 0.96.0, 0.96.1; 0.98.0, 0.98.1 (unfound); 0.100.0 (configured
origins). `sqex-proto::refusal::Code::OriginRefused` (70).
`sqexd::replica::ForwardFailed` -- `Unreachable`, `Limited(secs)`,
`Refused(code)` -- returned by `Forwarder::{forward, action}` in place
of a string, with `refused_forward` reading the wait off a 429;
`Server::forward_failed` maps it for a member at every forward site
(`/channel/post`, `/channel/create_at`, the carried actions), recording
the refusal; `note_origin_refusal` and `origin_held`; `run_homed`
records what `/peer/moved` and `/peer/folded` refuse and makes no
peering write to a held origin; `/status` gains `origins`. The chat
client says what `origin_refused` means. `HOLD_DEFAULT_SECS = 60`; a
stated wait holds for at most `MAX_HOLD_SECS = 3600`.

Tests: an origin whose peering limit is one an hour; the home carries
one account's Move and is refused the next with `rate_limited`, records
it under `/status` with the wait and a hold in force -- checked by
swallowing the refusal as before and watching nothing be recorded --
and a create carried for the refused account, which the origin refuses
for want of the Move, is answered `origin_refused`.

**Unfound** (§Unfound to §Hints, sqex 0.98.0). `sqexd`: `Server::{note_unfound, origin_unfound_until}`
and `Unfound { since, tries, next, why }` with `MAX_UNFOUND_SECS`
(= `MAX_HOLD_SECS`); `Server::reached` clears the record;
the SIP-59 home task skips an origin under a hold and notes a miss at
its two failure points (no address from `reach_by`, no connection);
`origins_value` adds the origins every account homed here hinted at
and the `unfound` field; `sqex status` prints `UNFOUND (why) n tries,
next in Ns`. Tests (`unfound_flow`): an origin hinted under a domain
nothing resolves is listed at once and tried four times in eight
seconds of a one-second cycle -- nine with the hold disabled, which is
the negative control -- with `why = no_address` and a next try in the
future; an origin whose address takes no connection is recorded
`unreachable`, and when it comes up at that address the next attempt
finds it, the record is gone and `reached` is set.

Run live on 2026-09-19 22:21 UTC, both exchanges on 0.98.0. The first
`sqex status` at trunk.exchange listed an origin nobody had seen: a
second stale hint, from another throwaway account, naming the same
stopped laptop exchange with **no domain** -- so no DNS lookup, no
resolver warning, and a `no_address` miss at `debug` every thirty
seconds since the fold runs (SIP-60 §A direct message opened twice), invisible until the rows listed hinted
origins. trunk.exchange logged one warn (`tries=1 next_in=30`) and
held off. Withdrawing the hint showed the one thing the text had not
said: the record was cleared only by an answer, and nothing would try
the origin again, so the row outlived its reason. 0.98.1 keeps records
only for origins some account still hints at, and the test withdraws
the hint and watches the row go.
