```
SIP: <number, assigned by an editor>
Title: <short imperative phrase, no trailing period>
Author: <name <email>>
Status: Draft
Type: <Standards Track | Informational | Process>
Layer: <Transport | Naming | Exchange | Application>
Created: <YYYY-MM-DD>
Requires: <SIP numbers, omit if none>
Replaces: <SIP numbers, omit if none>
```

## Abstract

One paragraph. What this changes, in terms a reader can act on.

## Motivation

The problem, and why the existing pieces do not already solve it. If another
component could plausibly own this instead, say which and why it does not.

## Specification

Normative. Wire formats byte by byte, with field widths and byte order. State
which behaviours are required and which are optional. A second implementer
should be able to work from this section alone.

## Rationale

Why this design and not the obvious alternatives. Record what was rejected —
the alternatives are the part future readers will re-derive otherwise.

## Compatibility

What breaks. Whether old and new peers interoperate, and in which direction.
For anything on the wire, state whether both implementations must move
together.

## Security considerations

What an attacker gains, what is disclosed to whom, and what this does not
protect against. "None" is almost never the honest answer.

## Reference implementation

Links, or "None yet". A Standards Track SIP cannot reach Active without one.
